import React from 'react';
import { MainView } from '@/components';

export default function Checkout(): ReactComponent {
  return (
    <MainView className="view-shared view-checkout">
      <h2>Checkout Page</h2>
    </MainView>
  );
}
