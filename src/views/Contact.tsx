import React from 'react';
import { MainView } from '@/components';

export default function Contact(): ReactComponent {
  return (
    <MainView className="view-shared view-contact">
      <h2>Contact Page</h2>
    </MainView>
  );
}
