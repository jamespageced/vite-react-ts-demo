import React from 'react';
import { MainView } from '@/components';

export default function Dashboard(): ReactComponent {
  return (
    <MainView className="view-shared view-dashboard">
      <h2>Dashboard</h2>
    </MainView>
  );
}
