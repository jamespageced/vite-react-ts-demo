import React from 'react';
import { MainView } from '@/components';

export default function About(): ReactComponent {
  return (
    <MainView className="view-shared view-about">
      <h2>About Page</h2>
    </MainView>
  );
}
